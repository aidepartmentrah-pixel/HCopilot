# HCopilot — Legacy Frontend Shell & Dashboard Refresh — Slicing Task Table

**Live document — edit this directly as work happens.** Same discipline
as `docs/development/ER UI Architecture Redesign/0. Slicing Task
Table.md`: status lives here, updated in place, dated log entries
appended per slice as real work lands — never rewritten silently.

**Status as of 2026-09-22: UI-T1 DONE, rest of Workstream T queued.**
This doc formalizes the slicing/estimation given to the user in chat
before any code was written. User confirmed: write this doc, then
start Workstream T first (self-contained, no backend calls needed).

## Background

Source notes: `source/Theme Edits.md` and `source/Dashboard Edit.md`
(copied verbatim from the user's own files, originally under
`Desktop/Slight Edit to the Old HCopilot/`).

**Both workstreams target the OLD (legacy) vanilla HTML/CSS/JS
frontend under `frontend/`, not the new React/TypeScript app under
`New Frontend (React Rewrite)`.** This was confirmed explicitly by the
user after some back-and-forth:

- `Theme Edits.md` is unambiguous on its own — it explicitly scopes
  itself to "the existing vanilla HTML/CSS/JavaScript HCopilot
  frontend" and explicitly forbids introducing React/TypeScript/Vite.
- `Dashboard Edit.md` is written in React terms (component names like
  `DashboardPage`/`MetricCard`, "Lucide React" icons) because it was
  originally drafted for the new frontend. The user wants its
  *ideas* — operational KPIs, a real welcome panel, activity/alerts/
  status panels — pulled out and re-slotted onto the **old**
  frontend's Home page instead, translated to vanilla JS render
  functions and local static SVG icons (no React, no component
  library).

Both source docs describe a React-flavored implementation
(`DashboardPage`, `MetricCard`, "Lucide React"); every slice below
reinterprets that as vanilla HTML/CSS/JS work against the existing
`frontend/js/*.js` + `frontend/css/*.css` + `index.html` pattern
already used throughout this codebase (e.g. `patient-details-view.js`'s
pure data-builder + render-function split is the model to follow, not
a new component framework).

## Current state (verified by reading the real files, not assumed)

- Home page today is exactly the "four-shortcut launcher" both docs
  describe replacing: a `.welcome-card` + four `.feature-card`s linking
  to Patients/Flow Prediction/Beds Display/Scheduling
  (`frontend/index.html:114-152`, `frontend/css/home.css`, 88 lines).
- The navbar/header (`frontend/index.html:35-110`) uses emoji icons
  throughout (🏠📈🧑‍⚕️📜🛏️📅📊⚙️⏻), a plain brand text lockup (no
  logo asset), and the Administrator/user badge is a visually separate
  block bolted onto the header's right edge.
- Brand colors are hardcoded purple gradients scattered directly across
  `frontend/css/base.css` (e.g. `#667eea`/`#764ba2` at the body
  background, `#5a67d8`/`#6b46c1` repeated at least 6 more times for
  header/nav-active/hover states) — there is no central token/variable
  system and no `data-theme` mechanism today. This matches what
  `Theme Edits.md` §4 predicted before any file was opened.
- Settings already has an established tabbed-section pattern
  (`.settings-tab` + `data-tab` + `showSettingsTab()`,
  `frontend/index.html:1971+`) that a new "Appearance" tab can reuse
  as-is rather than inventing new tab UI.
- No activity/event-log concept and no `/health`-style endpoint exist
  anywhere in the backend today — confirmed by there being no matching
  route in `backend/features/*/api.py` for either.

## How this relates to prior work

This reuses UI/testing conventions from the ER UI Architecture
Redesign (branch discipline, Playwright-first verification, dated log
entries, "propose options before implementing" for any genuinely
ambiguous design call) but is otherwise independent — no shared code
path with Pages A/B/C beyond one specific reuse noted in `UI-D7`
(the `triage_time IS NULL` waiting-time rule) and `UI-T3`/`UI-T4`
(local SVG/logo asset handling, a pattern this codebase hasn't needed
before since it has used emoji everywhere until now).

## Slice breakdown

Two independent workstreams — either can ship alone. Suggested order:
**T (Theme) before D (Dashboard)** — T is self-contained and lower
risk; D has two backend-touching slices (`D3`, `D8`) and one slice with
no existing precedent to reuse (`D6`), so starting there first would
mean making backend-design calls before the easier, fully-scoped work
is even done.

### Workstream T — Legacy Shell & Theme Refresh (`UI-T*`)

- **UI-T1** — Central CSS design tokens: introduce `:root` variables
  (brand scale, canvas, surface, text, border, semantic
  success/warning/danger) in `base.css`, replacing the scattered
  hardcoded purple hex values found above. No visual change yet — pure
  refactor, verified by screenshot diffing against current state.
- **UI-T2** — Navbar visual restyle using the new tokens: spacing,
  elevation (`box-shadow`), hover/active states, per Theme Edits.md
  §10–14. No markup/logic change.
- **UI-T3** — Replace emoji nav/action icons with a small local SVG
  set (Home/Flow Prediction/Patients/History/Care/Operations/
  Statistics/Settings/Logout, ~9 icons). New kind of work for this
  codebase — needs real icon files sourced and stored under
  `frontend/` (e.g. `frontend/assets/icons/`), not just CSS.
- **UI-T4** — Brand lockup: swap the plain text brand for the approved
  HCopilot logo mark (Option 5, from the images the user already
  shared) + tagline, stored as a local asset. Same "new asset" caveat
  as UI-T3.
- **UI-T5** — Integrate the Administrator/user badge
  (`frontend/index.html:100-109`) into the navbar as one visual unit
  instead of a detached block. Restyle only, `authLogout()` etc. stay
  untouched.
- **UI-T6** — Workspace background refresh: replace the current
  saturated purple body background with the lighter canvas + subtle
  decorative shapes. CSS-only (gradients/pseudo-elements, Theme Edits.md
  §21 Option A) — no new asset needed, avoids an air-gapped-asset
  question entirely.
- **UI-T7** — Three theme variants (Indigo/Teal/Burgundy Clinical) via
  `[data-theme]` token overrides on top of UI-T1's variables. Semantic
  colors (green/amber/red) stay theme-independent per Theme Edits.md
  §9 — must not be touched by this slice.
- **UI-T8** — Settings → Appearance tab: reuse the existing
  `.settings-tab`/`showSettingsTab()` pattern, add a 3-option theme
  picker with small visual previews, `localStorage` read/write, applied
  as early as possible on page load to avoid a flash of the wrong
  theme.
- **UI-T9** — Regression pass: every existing page/dropdown/route under
  all 3 themes, at 1366/1440/1600/1920px, plus a Playwright smoke test
  covering theme switch + persistence across reload.

### Workstream D — Home Dashboard (`UI-D*`, ideas adapted from `Dashboard Edit.md`)

- **UI-D1** — Page header refactor: "HCopilot Overview" title + live
  date/time + hospital/site name, hospital name sourced from frontend
  config rather than hardcoded (per Dashboard Edit.md §3).
- **UI-D2** — Compact welcome panel (~180–220px): time-based greeting +
  authenticated user's real name (from existing auth session data, not
  a placeholder) + short static message + one local background image
  asset (subtle, low-contrast, bundled locally).
- **UI-D3** — KPI data plumbing: confirm and wire the real sources for
  the 4 numbers (Active ER Patients, Occupied/Total Beds, Waiting
  Without Bed, Discharged Today) against endpoints already used by
  Beds Display/Patients/History. **Investigation slice first** — most
  of this is likely a thin client-side aggregation of existing calls,
  but confirm before assuming no backend change is needed.
- **UI-D4** — KPI card row: 4 shared-markup cards wired to UI-D3, each
  with its own independent loading/error/empty state (one API failing
  must not blank the whole row, per Dashboard Edit.md §19). No
  fabricated trend percentages.
- **UI-D5** — Quick Actions section: reuse the current Home page's
  existing `.feature-card` pattern almost as-is, reordered/relabeled
  per Dashboard Edit.md §7 (New ISBAR Entry primary, 3 secondary).
  Lowest-risk slice — closest to what already exists today.
- **UI-D6** — Recent ER Activity panel: derive a short (5–8 row) event
  list (arrived / ISBAR started / bed assigned / bed changed /
  discharged) from existing timestamped fields across
  patients/beds/history. **Biggest open unknown in this whole
  redesign** — no activity/event-log concept exists anywhere today
  (confirmed above); may need a small new backend aggregation endpoint
  rather than pure frontend synthesis. Do not build this slice's data
  approach without confirming it with the user first — same
  "propose before implementing" rule as everything else in this repo.
- **UI-D7** — Operational Alerts panel: waiting>5min (reuses the
  `triage_time IS NULL` rule already built for the ER Live-Roster
  Redesign — no new backend needed here), occupancy threshold alert
  (config-driven 80%/90% bands, not hardcoded in a component), and
  ER-roster API freshness/staleness alert.
- **UI-D8** — System Status panel + backend support: a genuinely new
  `GET /health`-style endpoint reporting backend/DB/Hospital-Directory-
  API/ER-current-visits-API connectivity (Dashboard Edit.md §11). Real
  backend work, not frontend surfacing of something that already
  exists — flag and confirm scope before building.
- **UI-D9** — Auto-refresh + "Last updated HH:MM" wiring for the new
  panels, without a full page reload.
- **UI-D10** — Responsive pass (1366/1440/1600/1920px, KPI row
  wrapping to 2-per-row, panels stacking) + Playwright coverage for the
  new Home page.

## Relative sizing (given to the user before this doc existed)

Reference unit: the ER UI Architecture Redesign (19 slices across
Pages A/B/C + 6 post-close feedback rounds, all DONE).

- **Workstream T ≈ 1× Page A** (6 slices). Entirely CSS/localStorage —
  zero backend, zero new pages. The lightest of the efforts scoped so
  far in this repo.
- **Workstream D ≈ 1–1.3× Page B** (the largest of the three ER pages
  once its follow-up rounds are counted). Two slices (`D3`, `D8`) touch
  backend and one (`D6`) has no existing pattern to reuse — everything
  shipped in the ER redesign was UI surfacing of already-built backend
  behavior; `D6`/`D8` are the first slices in this line of work that
  need new backend design, not just new UI over old data.
- **Combined ≈ 60–75% of the entire ER Architecture Redesign's total
  effort**, concentrated on one page plus the global shell rather than
  spread across three pages.

## Testing design

Same conventions as both prior redesigns:

- Playwright carries most of the verification weight for both
  workstreams — new spec files (`frontend/tests/theme-refresh.spec.js`,
  `frontend/tests/home-dashboard.spec.js`), real `npx playwright test`
  runs with real pass/fail counts pasted into this doc's log after
  every slice, not just claimed.
- Full existing suite re-run after every slice touching shared chrome
  (the navbar restyle in particular touches markup every other spec's
  `login()`/navigation helper depends on) — regression is the real risk
  for Workstream T specifically.
- Backend: only `UI-D3` (maybe) and `UI-D8` (definitely) touch the
  backend. If either needs new code, follow
  `backend/tests/test_er_redesign.py`'s pattern — real `TestClient(app)`,
  real DB writes, `finally` cleanup.
- Manual browser screenshot check for at least one slice per new visual
  surface (theme switch, each new dashboard panel) before calling it
  done, same discipline as the ER redesign's bed-graphic/tabs/sticky-
  pane rounds.

## Status table

| Slice | What | Depends on | Status |
|---|---|---|---|
| UI-T1 | Central CSS design tokens | — | DONE |
| UI-T2 | Navbar visual restyle | UI-T1 | NOT STARTED |
| UI-T3 | Local SVG icon set (replace emoji) | — | NOT STARTED |
| UI-T4 | Brand lockup (Option 5 logo + tagline) | — | NOT STARTED |
| UI-T5 | Integrate Administrator/user badge | UI-T2 | NOT STARTED |
| UI-T6 | Workspace background refresh | UI-T1 | NOT STARTED |
| UI-T7 | 3 theme variants via `[data-theme]` | UI-T1 | NOT STARTED |
| UI-T8 | Settings → Appearance tab | UI-T7 | NOT STARTED |
| UI-T9 | Regression + Playwright: Workstream T | UI-T2 – UI-T8 | NOT STARTED |
| UI-D1 | Header refactor (title/date/time/hospital) | — | NOT STARTED |
| UI-D2 | Compact welcome panel | — | NOT STARTED |
| UI-D3 | KPI data plumbing (investigation) | — | NOT STARTED |
| UI-D4 | KPI card row | UI-D3 | NOT STARTED |
| UI-D5 | Quick Actions section | — | NOT STARTED |
| UI-D6 | Recent ER Activity panel | Confirm approach first | NOT STARTED |
| UI-D7 | Operational Alerts panel | — | NOT STARTED |
| UI-D8 | System Status panel + `/health` endpoint | Confirm scope first | NOT STARTED |
| UI-D9 | Auto-refresh + Last updated wiring | UI-D4, UI-D6, UI-D7, UI-D8 | NOT STARTED |
| UI-D10 | Responsive pass + Playwright: Workstream D | UI-D1 – UI-D9 | NOT STARTED |

## Live status log

## UI-T1 — Central CSS design tokens, 2026-09-22

Extracted the hardcoded purple brand colors scattered across the
legacy frontend's CSS into a `:root` token block at the top of
`frontend/css/base.css`:

```css
:root {
    --brand-500: #667eea;
    --brand-600: #764ba2;
    --brand-700: #5a67d8;
    --brand-800: #6b46c1;
    --brand-accent: #4f46e5;
}
```

Real scope found by grepping, not assumed: 111 raw occurrences of these
5 hex values across **14 CSS files** (`auth.css`, `base.css`,
`beds.css`, `datasets.css`, `datetime-picker.css`,
`flow_prediction.css`, `home.css`, `modals.css`, `models.css`,
`patients.css`, `scheduling.css`, `settings.css`, `simulation.css`,
`statistics.css`) — all mechanically replaced with `var(--brand-*)`
references. Out of scope for this slice (left as-is, to revisit when
UI-T2/UI-T6 actually change these): a handful of `rgba(91,103,216,...)`
shadow/overlay values that are hand-decomposed RGB equivalents of the
same brand color rather than the hex literal itself.

**Bug caught before shipping**: the bulk `sed` replace was run in two
passes (one per new file, one to catch remaining occurrences in
`base.css` itself) — the second pass matched the token *definitions*
too, turning `--brand-500: #667eea` into the circular
`--brand-500: var(--brand-500)`, which is invalid CSS and would have
silently broken the entire brand palette (invalid custom properties
resolve to their inherited/initial value, so the whole header/button/
background system would have gone transparent or unstyled). Caught by
grepping for the `--brand-*: var(--brand-` self-reference pattern
before rebuilding, fixed by restoring the literal hex values in the
`:root` block only. Worth remembering for T2/T6/T7: any bulk CSS
find-replace touching a file that also *defines* the thing being
replaced needs a self-reference check afterward.

**Verified for real:**
- `docker compose build frontend && docker compose up -d frontend` —
  built and started clean.
- Screenshot of the logged-in Home page at 1440×900 — visually
  identical to before the refactor (same purple header/background
  gradient, same button colors, same active-nav state) — pure token
  extraction, zero visual change, as intended.
- Full Playwright suite: **25/25 passed** (1.0m), no regressions.

Next: UI-T2 (navbar visual restyle) is the first slice with an actual
visual change — will screenshot before/after and check with the user
before calling it done, same as every other visual decision in this
project.

## Related

- `docs/development/ER UI Architecture Redesign/0. Slicing Task Table.md`
- `docs/development/ER Live Roster Redesign/0. Slicing Task Table.md`
- `source/Theme Edits.md`, `source/Dashboard Edit.md` — original notes
