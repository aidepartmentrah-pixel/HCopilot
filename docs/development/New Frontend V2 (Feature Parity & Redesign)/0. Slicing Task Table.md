# New Frontend V2 — Feature Parity & Redesign — Slicing Task Table

## Mission

`frontend-new/` shipped with NF0–NF9 (see `../New Frontend (React Rewrite)/0.
Slicing Task Table.md`) but simplified too aggressively in places and is
missing one page outright. This is the second wave: restore the clinical/
operational depth of the old frontend (`../../../frontend/`) on every page,
apply a real branded shell, and add the missing Predictions page — all inside
`frontend-new/`, without regressing anything NF0–NF9 already shipped.

Full source spec (verbatim, do not paraphrase from memory — read it):
`source/11. New Stack Missing Functions.md`. It contains, per page, a "Locked
Decisions" list and a "Definition of Done" checklist — those are the actual
acceptance criteria for each slice below, not this table's summaries.

Kickoff prompt for the session that executes this: `KICKOFF_PROMPT.md` (also
saved to `C:\Users\it\Desktop\HCoplilot Promtps\`).

## Standing rules (apply to every slice)

- **Never fabricate data.** The source spec's mockup images (and the two
  ChatGPT-generated logo images at `C:\Users\it\Downloads\ChatGPT Image Sep
  22, 2026, *.png`) are layout/flow/"feel" references only. Every number,
  label, alert, or trend shown in the UI must trace to a real backend field —
  the source spec itself says this explicitly, repeatedly, per page ("Do not
  fabricate statistics purely for visual completeness", etc.). If a mockup
  implies data that doesn't exist yet (a % trend with no historical baseline,
  a PACS/LIS status HCopilot doesn't integrate with), omit it or use the
  documented empty/unavailable state — never invent it.
- **No emoji as UI graphics.** Lucide React only, per the source spec's own
  icon-system section, and per the existing `frontend-new` convention.
- **CSS Modules, no styling library** — same as NF0–NF9.
- **`dir="auto"` on patient-name-bearing fields** — Arabic names are a
  first-class case on every page touched here, not just ISBAR.
- **Air-gapped**: any new dependency (a charting library for Statistics/
  Predictions, the two logo PNGs, any new font) must be locally bundled and
  re-verified against `e2e/airgapped.spec.ts`. Flag a new runtime dependency
  before adding it rather than silently vendoring one.
- **A slice is not done until its tests are, and both pass twice in a row**
  (this project's own standing discipline from NF8 — a fix that only "looks"
  fixed once isn't trusted here).
- **Testing shape per slice**: Vitest + RTL for new components/pure logic
  (aggregation math, formatting, control state machines) — colocated, same
  as existing `frontend-new/src/**/*.test.tsx`. Playwright e2e against the
  real backend for user flows — extend the existing spec files or add one
  per page, same `fullyParallel: false, workers: 1` convention (the
  `/api/patients/next-ids` race is still unfixed backend-side). Where a
  slice's UI depends on a backend endpoint not yet exercised by
  `frontend-new` (e.g. Hospital Directory settings, model training/
  promotion, accounts), add a small smoke check (a Playwright test or a
  quick `curl`/pytest hit) that confirms the endpoint's real response shape
  *before* building the page against an assumed shape — don't guess the
  contract from the old frontend's JS and hope.
- **Don't touch `frontend/` (old) or `backend/` behavior** unless a slice
  explicitly requires a real, missing backend capability — check first
  whether the data already exists via another endpoint (most of this spec's
  "missing" functionality is old-frontend-only UI, not missing backend).
- **One commit per completed slice** (or per natural sub-slice for V2.2),
  same granularity discipline as the first mission's NF-slices. Do not merge
  to `main` or push — that decision belongs to the user.
- Log every slice here the same way NF0–NF9 were logged: a dated entry under
  the slice with what was actually done, real command/test output, bugs
  found, scope decisions made. This table is the authoritative progress
  record — update it as you go, not at the end.

## Status

Status as of 2026-09-22: IN PROGRESS. V2.0 done (see log). Working
autonomously through the remaining slices in dependency order; the user is
away and asked not to be checked in with mid-flight.

## Slice Index

| Slice | Name | Depends on | Status |
|---|---|---|---|
| V2.0 | Global Shell & Branding | — | Done (2026-09-22) |
| V2.1 | Home / Dashboard | V2.0 | Done (2026-09-22) |
| V2.2a | ISBAR Entry — Workflow Scaffolding | V2.0 | Not started |
| V2.2b | ISBAR Entry — Clinical Controls | V2.2a | Not started |
| V2.2c | ISBAR Entry — Section Content Parity | V2.2b | Not started |
| V2.3 | Live ER — Placement Board | V2.0 | Not started |
| V2.4 | History — Master/Detail | V2.0 | Not started |
| V2.5 | Statistics — Analytics System | V2.0 | Not started |
| V2.6 | Settings — IA Rework & Feature Parity | V2.0 | Not started |
| V2.7 | Predictions — New Module | V2.0 | Not started |
| V2.8 | Cross-cutting Regression & Polish | ALL above | Not started |

---

## V2.0 — Global Shell & Branding

Source: spec section "Theme & Top Bar".

**Scope**: Rebuild `AppShell`/`TopNavigation` as the indigo/navy branded
shell (spec §1–§13): HCopilot icon (pick/adapt from the two Downloads PNGs,
bundle locally as SVG/PNG under `src/` — do not reference `Downloads/` at
runtime), wordmark, nav, global search input (UI + wiring to whatever search
scope is genuinely supported today — do not ship a search box that does
nothing silently; if no backend search endpoint exists yet, say so in the
log and either scope it to client-side filtering of already-fetched data or
mark it visually present-but-disabled with a real reason), notification
bell (real unread state if derivable, otherwise an honest empty state — not
a fake badge), real authenticated user identity block (name/role from the
actual session, graceful initials-only fallback). Token/spacing scale
already exists in `design-system/tokens.css` from NF8.2 — reuse it, don't
refork it.

**Out of scope**: per-page content changes (that's V2.1–V2.7).

**Definition of Done**: spec's own "Locked Direction" + top-bar DoD items,
minus anything flagged as unsupported-by-backend in the log entry.

**Testing**: Vitest for `TopNavigation` (renders logo/nav/search/bell/user,
active-route highlighting, keyboard-accessible nav). Playwright: extend
`e2e/smoke.spec.ts` (or a new `e2e/shell.spec.ts`) asserting the shell is
byte-identical in structure across all 6+ routes (Home/ISBAR/Live
ER/History/Statistics/Settings/Predictions once V2.7 lands). Re-run
`e2e/airgapped.spec.ts` — new logo asset must resolve to `localhost`, never
an external URL.

### Log — 2026-09-22

**Built**: `HCopilotMark` (original locally-bundled SVG icon under
`src/components/brand/` and `public/favicon.svg` — a geometric flowing-H +
`+` accent adapted from the two reference PNGs' *concept*, not traced from
them, consistent with this mission's no-GPT-assumption rule since it's a UI
icon, not clinical data). Branded indigo/navy `TopNavigation` (new
`--shell-*` tokens in `tokens.css`, additive — nothing existing renamed).
`GlobalSearch` (client-side filter over `usePatients()` + `useHistory()`,
already-fetched data — no new backend endpoint; selecting a result opens
`/history/:stayId`, which `usePatientDetails` already resolves for both
active and discharged stays). `NotificationBell` (honest empty state, zero
fake badge — no alert source exists yet; takes an `items` prop V2.1 will
wire real alerts into once the alert-rule module exists). `UserMenu` (real
identity + Sign Out).

**Scope decision — added a real login/session layer (bigger than "top
bar")**: `frontend-new` had **no authentication at all** — no login page,
no session, direct landing on Home. The spec's "authenticated user
identity... never hardcode Dr. Sarah Chen... degrade gracefully" presumes a
session exists to degrade *from*. There was nothing to degrade from, and
faking a name would violate the mission's own never-fabricate rule harder
than building the real thing. The backend already fully supports this
(`auth/api.py`'s `/login`, unchanged, verified against `users_manager.py` —
seeded `admin`/`admin` account, no token/cookie, just returns the user row,
same contract the *old* frontend's `auth.js` already relies on via
localStorage). Built: `AuthProvider`/`useAuth`/`authContext`
(`src/app/providers/`), `LoginPage` (`src/features/auth/`), gating `App.tsx`
before `AppProviders` mounts. No role/section-based nav filtering was
added — the V2 spec text read so far never asks for it, and old frontend's
`sections`/`settings_tabs` gating is Settings/Accounts territory (V2.6),
not this slice. To avoid this rippling into all 8 other existing e2e spec
files, added `e2e/global-setup.ts`: logs in once directly against the real
backend (bypasses the frontend proxy/webServer-readiness ordering
entirely) and writes a `storageState` every spec now starts from
(gitignored, regenerated per run). `e2e/shell.spec.ts` separately covers
the actual login form (success + real 401 error message) by opting out of
that default state.

**Backend gap flagged, not faked**: `/health` (`backend/app.py:97-100`)
returns only `{"status": "ok"}` — no DB/Hospital-Directory/ER-roster
breakdown. V2.0 doesn't consume it yet (shell has no System Status panel —
that's V2.1's Home dashboard). Flagging now so V2.1 doesn't assume `/health`
already reports all four services; it may need either a real backend
change or per-service checks derived from the hooks that already exist
(`useErRoster` already surfaces hospital-directory-down as a normal
`status` field, not an error).

**Search copy**: placeholder says "MRN" per spec wording; the schema has no
separate MRN field, so it searches `patient_id` (old system's closest
equivalent) — noted here rather than silently reinterpreting spec copy.

**Verification** (`frontend-new/`, real backend via `docker compose`, not
mocked):
- `npm run lint` — 0 warnings (split `AuthProvider`/`useAuth`/`authContext`
  into separate files to keep the existing fast-refresh-clean convention
  `ToastProvider` already established).
- `npm run build` — clean `tsc -b && vite build`, no type errors.
- `npm run test` — **63/63, run twice in a row** (18 files: 3 new —
  `TopNavigation.test.tsx`, `AuthProvider.test.tsx`, `LoginPage.test.tsx` —
  plus `App.test.tsx` rewritten for the two real states, signed-out and
  signed-in, instead of the old single assertion that no longer holds).
- `npx playwright test` (real backend, stopped the `frontend-new` container
  during dev so Playwright's own `webServer` served a fresh local build
  instead of a stale one — the exact gotcha the README already warns
  about) — **30/30 relevant specs, run twice in a row**
  (shell/smoke/airgapped/isbar/live-er/history/statistics/settings/rtl).
  `visual-regression.spec.ts` now fails on 6/6 baselines — **expected**,
  logged not fixed: V2.8 owns the one real re-baseline after every page's
  content has also changed, re-baselining per-slice would just mean
  re-baselining 8 times.
- Found `history.spec.ts`'s "acuity filter narrows the table" failing
  (expects ≤20 rows after filtering, table has 22) — confirmed via
  `git stash` + rebuild that this **already fails identically on the
  pre-V2.0 baseline** (live log-patients data has grown past what the test
  assumed). Pre-existing, unrelated to this slice, not fixed here — belongs
  to V2.4 (History).
- Rebuilt and restarted the real `hcopilot-frontend-new-1` Docker image
  (`docker compose build frontend-new && docker compose up -d
  frontend-new`) — healthy, `curl localhost:8083/` → 200, serving this
  slice's code, not a stale build.

**Out of scope, as planned**: no per-page content changes (V2.1–V2.7); no
role-based nav/section filtering (not asked for by the spec text covering
this slice, and would belong to V2.6 if it is later).

---

## V2.1 — Home / Dashboard

Source: spec section "The Dashboard / Main Page".

**Scope**: Replace the 4-shortcut launcher with the operational dashboard
(spec §1–§27): welcome panel (real user name, no hardcoded "Dr. Sarah
Chen"), 4 KPI cards (Active ER Patients, Occupied Beds, Waiting Without Bed,
Discharged Today — all from existing endpoints already consumed elsewhere in
`frontend-new`, e.g. `useBeds`/`useErRoster`/`usePatients`), Quick Actions
(reuse the 4 existing nav destinations), Recent ER Activity (derive from
existing timestamped fields only — arrival/bed-assigned/departed — do not
build a new event-log table), Operational Alerts (only the 3 rules the spec
defines: new-patient->5min-no-ISBAR, occupancy threshold, ER-roster
staleness — thresholds centralized in one constants file, not scattered),
System Status (HCopilot backend + DB + Hospital Directory API + ER roster
API only — check what `/health` currently reports before assuming it covers
all four; if it doesn't, that's a small real backend gap to flag, not fake
in the frontend).

**Explicitly do not**: fabricate trend percentages, invent PACS/LIS status,
invent staff names.

**Testing**: Vitest for the alert-rule functions (occupancy threshold math,
5-minute-no-ISBAR detection, staleness detection) — these are pure logic,
test them directly with fixed timestamps, not through the UI. Playwright
covering: KPIs render real values, an alert appears only when its real
condition is met (seed/verify against actual test data, don't mock), each
panel degrades independently when its own query fails (kill one endpoint's
data path, confirm the other 3 panels still render). Visual regression
baseline reset for Home.

### Log — 2026-09-22

**Built**: `HomePage` rewritten around `WelcomePanel`, `KpiRow`,
`QuickActions`, `RecentActivityPanel`, `OperationalAlertsPanel`,
`SystemStatusPanel`, `DateTimeHospital` (all `src/features/home/`). Pure,
directly-tested logic split out: `alertRules.ts` (the 3 real alert rules),
`kpis.ts` (`countDischargedToday`), `recentActivity.ts`
(`buildRecentActivity`), `greeting.ts`. Thresholds centralized in
`constants.ts` per the spec's own instruction. `useOperationalAlerts()`
combines the rules with live data and is now shared by both
`OperationalAlertsPanel` **and** the shell's `NotificationBell` (V2.0 left
that bell wired for real content once this existed — now it shows the same
real alerts, not a duplicate/divergent copy of the logic).

**KPI sourcing** (all from endpoints already used elsewhere, no new ones):
Active ER Patients = `usePatients().patients.length`; Occupied Beds =
`useBedStats()` (`occupied`/`total_beds`/`occupancy_rate` are already
server-computed); Waiting Without Bed = `useBedlessPatients().total`
(matches the spec's own definition of that KPI exactly — bedless *active*
patients); Discharged Today = `useHistory()` filtered client-side to
today's local calendar date (`countDischargedToday`, tested with fixed
timestamps — this is the one KPI with real date-boundary logic worth
testing directly).

**System Status — real gap, not padded**: confirmed via `curl
localhost:8090/health` that it returns only `{"status":"ok"}` (backend
liveness, no DB check per its own docstring). **Database is left out of
the panel** rather than inferred from unrelated query success — that would
silently conflate "some page's data fetch worked" with "the database is
up", which is a different, weaker claim. Also confirmed
`/api/hospital-directory/er/current-visits` *is* both the "Hospital
Directory API" and "ER Current Visits API" the spec lists as separate rows
— same one integration in this system's actual architecture, so this
panel shows one real row for it instead of two cosmetically-distinct rows
that would always report the same thing. `/health` isn't proxied by Vite's
dev/preview server by default (only `/api` was) — added a `/health` proxy
entry to both `server` and `preview` blocks in `vite.config.ts`.

**Welcome panel — no photo, and said so**: the spec asks for a "restrained
clinical photograph". No such licensed asset exists in this repo, and the
two reference PNGs are GPT-generated mockups, not photography this
mission's own rules allow using as literal content. Used a small inline
abstract SVG (translucent circles in the brand gradient) instead of
pretending a fabricated image is a real photo — logged as a deliberate
substitution, not silently done.

**Hospital name**: added `VITE_HOSPITAL_NAME` (frontend-new/.env(.example),
empty by default) per the spec's own instruction that this come from
config, not a hardcoded string. This dev deployment has no real configured
site name, so `DateTimeHospital` simply omits that line when unset rather
than inventing "Al-Rassoul Al-Aazam Hospital" from the mockup text.

**Recent Activity scope**: only 3 events — Patient Arrived
(`arrival_time`), Bed Assigned (`bed_occupation_time`), Discharged
(`departure_time`, from history). The spec's example list also mentions
"ISBAR started" and "bed changed", but there is no discrete timestamp for
either in `isbar.ts`/`patient.ts`/`history.ts` — approximating them from
other fields would be a fabricated precision this mission's rules don't
allow, so they're left out.

**Verification** (`frontend-new/`, real backend):
- `npm run lint` — 0 warnings. `npm run build` — clean, no type errors.
- `npm run test` — **80/80, run twice in a row** (17 new: `alertRules.ts`
  11, `kpis.ts` 3, `greeting.ts` 3).
- `npx playwright test` (container stopped during dev, local preview
  build served fresh, same as V2.0) — **35/35 relevant specs, run twice in
  a row**, including new `e2e/home.spec.ts` (KPIs render real values and
  cross-check against a live `curl /api/beds/stats`; alerts panel content
  is honest whatever the live state is; Quick Actions navigate correctly;
  killing `/api/beds/stats` via route interception leaves the other 3 KPIs
  and System Status rendering normally — real panel-independence, not
  asserted, actually forced and observed) and an updated `shell.spec.ts`
  bell test (V2.0's version assumed an always-empty bell; this dataset's
  demo arrival timestamps are stale enough that the awaiting-ISBAR rule
  genuinely fires, so the test now verifies honesty — a badge count always
  matches that many real, non-empty messages — rather than assuming a
  fixed state). `smoke.spec.ts`/`airgapped.spec.ts`'s existing
  `getByRole('heading', {name:'HCopilot'})` assertions kept passing
  unchanged against the new "HCopilot Overview" title — confirmed
  Playwright's default `name` matching is substring, not exact, so no
  edit was needed there.
- Rebuilt and restarted the real `hcopilot-frontend-new-1` Docker image —
  healthy, `curl localhost:8083/` → 200.

**Out of scope, as planned**: no fabricated trend percentages, no
PACS/LIS/lab status, no invented staff names. Visual-regression baseline
still deferred to V2.8 (now 7/7 stale, expected).

---

## V2.2a — ISBAR Entry — Workflow Scaffolding

Source: spec section "ER ISBAR Entry", §1–§11, §29–§32, §40.

**Scope**: Roster-emphasis-before-selection / workflow-emphasis-after
layout change, persistent `SelectedPatientBanner` (sticky per §32),
6-step `ISBARStepIndicator`, accordion auto-progression on Continue
(validate → mark complete → collapse → open next → scroll into view),
section status badges (Not started/In progress/Complete/Missing required
fields), unsaved-changes guard on Change Patient (§38). This slice is
purely the workflow shell — it reuses whatever fields the ISBAR sections
already have today; it does not yet add the restored clinical controls
(that's V2.2b) or restore removed fields (V2.2c).

**Testing**: Vitest for the step-progression state machine (given N sections
with mixed valid/invalid state, which one is "current"). Playwright: section
1 starts active → Continue validates and blocks on missing required fields →
valid Continue collapses section 1 and opens section 2 → previously-completed
section reopens on click and its status updates if it becomes invalid →
Change Patient with unsaved changes shows the confirmation dialog from §38.

---

## V2.2b — ISBAR Entry — Clinical Controls

Source: spec section "ER ISBAR Entry", §12–§17, §23–§25, §33–§35.

**Scope**: Build the reusable clinical-control components named in the
spec's §40 component list and wire them into whichever sections currently
use a plain input/dropdown for these: `ESIScaleSelector` (full ESI 1–5
segmented, severity-colored, §12), `UnitInput` (suffix-anchored units,
always visible, §15), `BloodPressureInput` (SBP/DBP composite, §16),
`PainScaleSelector` (0–10 visual scale with semantic grouping, §17),
`ClinicalSegmentedControl` / `YesNoControl` (already exists as
`YesNoToggle` from NF — reuse/extend rather than duplicate),
`ClinicalCheckboxTile` (rectangular multi-select tile, §19/§20). Read-only/
auto-populated field styling (§13).

**Testing**: Vitest per component — ESI selector stores/reads the right
value and is keyboard-operable; BP input stores SBP and DBP as separate
fields; pain scale stores the numeric 0–10 value and applies the correct
semantic band class; unit input keeps the suffix visible after a value is
typed. Playwright: each control appears correctly inside the real Vital
Signs/Patient & Arrival sections and round-trips through a real save.

---

## V2.2c — ISBAR Entry — Section Content Parity

Source: spec section "ER ISBAR Entry", §18–§22, §41.

**Scope**: Per §41's own audit procedure — for Situation, Background, and
Focused Assessment/Recommendation, diff the old frontend
(`frontend/js/patient-isbar-form.js` + `patient-isbar-fields.js`) and the
current backend model against what `frontend-new`'s current sections
render, and restore anything present in old+backend but dropped in new:
Current Clinical Status tiles, Immediate Concerns multi-select, Past
Medical History tiles, Surgical/Procedure History Yes/No with conditional
reveal, Allergies Yes/No/NKA with conditional reveal, Isolation
Precautions' existing conditional fields. Do not invent fields the old
frontend/backend doesn't have.

**Testing**: Vitest for each new multi-select/conditional-reveal component.
Playwright: every restored field persists through save and reload (this is
the regression-prone part — a field that renders but doesn't actually bind
to the form schema is worse than an honestly-missing field).

---

## V2.3 — Live ER — Placement Board

Source: spec section "LIVE ER".

**Scope**: One `PlacementCard` family (§9, §18, §43–§44) covering available
bed / occupied bed+patient / waiting-no-bed patient — replace the current
generic empty-card look. Prominent bed SVG (locally bundled, no emoji —
§8). Occupied cards show real patient identity (name/ID/age/gender — §11,
this is the main functional gap today, not just styling). Waiting-patient
cards use a seated-person SVG, not emoji (§14–§16), and fix the malformed
waiting-duration formatting bug the spec calls out explicitly (§16 —
"Waiting 20814m" must never render; format as `1 h 12 min` etc.). Ward
rails get real Previous/Next controls instead of a bare scrollbar (§19–
§20). Selection + assignment flow (§22–§26): click waiting patient →
select → click available bed → confirm → mutate → board updates. Patient
detail drawer (§29) instead of navigating away. Correct the "waiting
without triage" mislabeling per §5 — check what the underlying condition
actually is before choosing wording.

**Testing**: Vitest for the waiting-duration formatter specifically (feed it
the known-bad large-minute case and confirm it never regresses), and for
the placement-card state derivation (available/occupied/waiting →
card props). Playwright: rail next/prev works and disables at boundaries;
selecting a waiting patient then an available bed offers assignment and
completes it end-to-end against the real backend; occupied-card click opens
the drawer with correct patient identity; soft-departure confirmation and
removal from the board.

---

## V2.4 — History — Master/Detail

Source: spec section "History".

**Scope**: Resizable split-screen (§6–§10, §43): historical stays table on
the left, `Selected Stay Preview` on the right, draggable divider,
persisted width via `localStorage` (per-viewer convenience — consistent
with this project's existing browser-storage discipline). Investigate the
`Patient #10000003`-instead-of-name issue per §34 as a real data-mapping
question first (is the name available via another field/endpoint, or
genuinely missing) — do not hardcode or fabricate names either way, and
record the actual answer in this doc's log. Friendly date formatting
(§14, reuse `humanize.ts`). Restore View/Edit/Delete row actions (§17,
§20–§21) — Edit only if a real destination exists; do not build a
duplicate editor. Read-only ISBAR preview accordions with Recorded/Not
Recorded/Partially Recorded state (§25–§29) reusing V2.2's section-display
logic where the data shapes match. Open Full Record (§31).

**Testing**: Vitest for the resizable-panel width persistence (reuses the
existing `ResizablePanel` component from NF — extend, don't rebuild) and
for the Recorded/Not-Recorded derivation logic. Playwright: row click
selects and previews without navigation; divider drag changes and persists
width across a reload; delete requires confirmation and updates the table;
Arabic names render correctly in both the table and the preview header.

---

## V2.5 — Statistics — Analytics System

Source: spec section "Statistics".

**Scope**: This slice needs a charting library decision first — flag it,
don't silently vendor one. Requirements from the spec: responsive,
bar/line/donut/table, tooltips, TypeScript, bundleable for air-gapped
(§46). Build the reusable `AnalyticsCard` (§8–§11) with a per-card
chart-type switcher restricted to that dataset's valid views (§9, §50) and
localStorage-persisted preference (§11). Global date-range filter (§3–§4).
Replace the current horizontal-progress-bar-everywhere pattern with the
spec's actual per-dataset defaults (§13, §14, §15, §17–§21): Wait Time and
LOS as column charts, Acuity as donut+table with the shared ESI palette
(§16 — this palette must match ISBAR's ESI selector from V2.2b, not be
reinvented), Arrivals with an Hour-of-Day/Day-of-Week toggle, Most Reported
Concerns and Safety Risks as ranked bar cards. Keep the existing
documentation-coverage/sample-size disclosure (§22–§23) — don't drop it
while restyling.

**Testing**: Vitest for every aggregation function (wait-time bucketing, LOS
bucketing, arrivals-by-hour, arrivals-by-weekday, concern/safety-risk
counts) — these are the highest-value tests in this slice since a chart
library swap is easy to get visually right and numerically wrong. Playwright:
chart-type switcher only offers valid views per dataset; date-range filter
changes all cards consistently; tooltips show real values; table-view
fallback renders exact numbers for every card. Re-run `airgapped.spec.ts`
once the chart library is in — this is the slice most likely to
accidentally pull a CDN font/script.

---

## V2.6 — Settings — IA Rework & Feature Parity

Source: spec section "Setting".

**Scope**: Replace the current Settings layout with the left-sidebar IA in
§2/§39 (Resources / System / Access groups). Explicitly **remove**: any
Reset UI (§3, §38 — there must be none, not even hidden), normal-user
Feature Visibility (§36), Patient Visibility (§37 — don't add it, it
doesn't exist yet either). Restore, reusing the backend that already
exists from the just-committed Hospital Directory integration
(`backend/features/hospital_directory/`, `ExternalApiSettings` — no new
backend needed here, this is a frontend gap only): Integrations →
Hospital Directory (connection form, Test Connection, credential-safe API
key field per §14, Middle-Name Guess List per §15). AI & Models: merge
into one destination with Model Registry + Training internal tabs (§17–
§28) — Training already has a real backend
(`backend/features/model_training/`, per the git history's "Add a
training engine + API for the Flow Prediction XGBoost model" commit) so
this is largely wiring the existing training UI into the new IA, not
building it from scratch; check `frontend/js/` for what already calls it.
Accounts & Permissions (§29–§35) — confirm the backend
(`backend/features/auth/`) actually supports role/permission CRUD before
building the UI around an assumed shape (smoke-test the endpoints first).

**Testing**: Playwright covering: the new left-nav structure and that Reset
is genuinely absent from every code path (not just hidden by CSS); Hospital
Directory save/Test Connection round-trips against the real
`ExternalApiSettings` row; model Promote requires confirmation and updates
the live-model badge; account CRUD with confirmation on delete. Backend
smoke check (pytest or a direct `curl` against a running `docker compose`
stack) for each restored endpoint's actual response shape before the page
is built against it, per this doc's standing testing-shape rule.

---

## V2.7 — Predictions — New Module

Source: spec section "Model Prediction Page (A Complete Missing Page as Well)".

**Scope**: New top-level nav item `Predictions` (§1, distinct from the
Settings → AI & Models admin area — do not merge these, §2). `Patient Flow`
as the one real product for now (§4, §27 — no placeholder tabs for
unbuilt future products). Forecast horizon control (30/60/90 days only —
confirm these are what `backend/features/flow_prediction/api.py` actually
supports, don't assume, §7). KPI row (Average/Max/Min Daily Patients,
Total Historical Records — §9–§11). Historical-vs-Forecast chart reusing
V2.5's charting library (§13–§21): solid line for historical, dashed for
forecast, a clearly marked forecast-boundary annotation, no confidence
band unless the backend genuinely returns one. Production-model metadata
panel (§24–§25, secondary to the chart, not the page identity). Empty
states for no-production-model / insufficient-data / generation-failure
(§29–§31) — do not render a fake/zero chart in any of these cases.
Automatically uses the live/promoted model; no model-switching exposed to
normal users (§3, §33 — that stays in V2.6's admin area).

**Testing**: Vitest for the historical/forecast series-shaping and the
forecast-boundary placement logic. Playwright: horizon switch re-requests
and updates chart+KPIs together; no-production-model state renders
correctly when nothing is promoted; a promoted model (via V2.6's Promote
flow) correctly becomes what Predictions serves, without a manual
frontend switch.

---

## V2.8 — Cross-cutting Regression & Polish

**Scope**: Once V2.0–V2.7 are in — full visual-regression re-baseline
(`e2e/visual-regression.spec.ts`, every screenshot will have changed, that's
expected), full Vitest + Playwright suite run twice consecutively (this
project's own "confirmed by running the entire suite twice, not just the
touched specs" discipline from NF8.4), an accessibility pass (keyboard
nav through every new selector/tile/drawer, focus states, no
color-only status), a full `airgapped.spec.ts` re-verification now that a
charting library and new image assets are in, and a README.md update
documenting the new components/architecture decisions made across V2.0–
V2.7 (mirroring how NF0–NF9 ended with the current README).

**Testing**: This slice *is* the testing pass — no new features. Record the
two consecutive full-suite runs' results in this doc's log the same way
NF8.4 did ("32/32 both times").

