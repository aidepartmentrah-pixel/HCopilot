# Package marker — exposes this directory as a Python module.
# ward_census: computes and permanently records the daily per-ward patient
# census (currently-occupied + discharged-today), so historical ward load
# survives even though DailyPatients only reflects live state.
