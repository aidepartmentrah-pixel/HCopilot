# Package marker — exposes this directory as a Python module.
# staff_management: CRUD for doctors (Doctors.csv), nurses (Nurses.csv),
# shifts (Shifts.csv), and rotation groups (Groups.csv).
# Staff absence toggling and per-staff patient-count tracking are handled here.
