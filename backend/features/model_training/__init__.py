# Package marker — exposes this directory as a Python module.
# model_training: retrains, versions, and manages the Flow Prediction XGBoost
# model from an API — wraps the same FlowDataProcessor pipeline the trained
# model is already served from (see features/flow_prediction/).
