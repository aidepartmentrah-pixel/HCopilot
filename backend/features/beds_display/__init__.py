# Package marker — exposes this directory as a Python module.
# beds_display: manages hospital beds (EDbeds.csv) and exposes their live status.
# Bed status (Available / Occupied / Under Repair) is computed at read-time from
# the physical condition column in EDbeds.csv and the patient_bed relation table.
