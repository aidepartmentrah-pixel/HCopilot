# Package marker — exposes this directory as a Python module.
# statistics: computes aggregate metrics from DailyPatients.csv and LogPatients.csv —
# overview KPIs, waiting-time distributions, acuity breakdown, throughput by hour/day,
# top chief complaints, and current vital-sign summaries.
