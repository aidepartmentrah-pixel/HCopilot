# Package marker — exposes this directory as a Python module.
# auth: user authentication and account management backed by Users.csv.
# Passwords are stored as SHA-256 hashes; roles control which sections and
# settings tabs a user can access in the frontend.
